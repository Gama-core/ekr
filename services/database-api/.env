# database-api\.env

# --- Database Connection (Required for app.database) ---
DB_HOST="162.19.255.83"
DB_PORT="5432"
DB_USER="mdx"
DB_PASSWORD="poiu7erty"
DB_NAME="mdx"

# --- Core Application Control ---
# This service doesn't need to load other settings from the DB itself,
# but we keep this flag to maintain the pattern and potentially for future use.
# For now, it only uses the DB credentials to connect.
LOAD_CONFIG_FROM_DB=False